export enum shipType {
    Carrier = 'Aircraft carriers',
    Submarine = 'Submarine',
    Destroyer = 'Destroyer',
    Cruiser = 'Cruiser',
    Frigate = 'Frigate'
}

export enum playerType {
  Human = 'Human',
  Bot = 'Bot'
}

export enum Status {
  NotStarted = 0,
  Started = 1,
  Terminated = -1,
  Finished = 2
}